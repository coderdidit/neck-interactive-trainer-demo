{
  "name": "nasit-demo",
  "version": "1.0.0",
  "description": "neck and sholders interactive trainer",
  "main": "src/index.js",
  "engines": {
    "node": ">=14.17.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "parcel src/index.html",
    "build": "rm -rf build && parcel build src/index.html -d build"
  },
  "author": "Marcin Kuzdowicz",
  "license": "ISC",
  "dependencies": {
    "@tensorflow/tfjs": "^3.7.0",
    "parcel-bundler": "^1.12.5"
  },
  "devDependencies": {
    "concurrently": "^6.2.0"
  }
}
